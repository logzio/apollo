apollo {
  api {
    listen = "0.0.0.0"
    port = 8081
    secret = "SuperTestingSecret"
    disableApiServer = "true"
  }

  kubernetes {
    monitoringFrequencySeconds = 1
    healthFrequencySeconds = 1
  }

  scm {
    githubLogin = ""
    githubOauthToken = ""
  }

  slave {
    slaveId = "tahat123"
    keepaliveIntervalSeconds = 1
    isSlave = "true"
    slaveCsvEnvironments = "1"
  }
}